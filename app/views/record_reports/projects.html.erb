<%= render 'record_reports/header' %>
<div class="row">
  <div class="my-chart-box">
    <div class="box-header with-border">
      <h3 class="box-title"><%= t('sections.reports.project_report') %></h3>
      <%= link_to t('common.export'), projects_record_reports_path(format: 'xls', date_range: params[:date_range], user_id: params[:user_id], project_id: params[:project_id], group_type: params[:group_type], company_id: params[:company_id]) %>
    </div>
    <div class="box-tools">
      <%= form_tag projects_record_reports_path, method: :get do %>
      <%= render 'record_reports/search' %>
      <% end %>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <%= render 'record_reports/projects/bar' %>

    </div>
  </div>
</div>
