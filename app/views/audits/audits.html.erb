<%= render 'audits/header' %>
<div class="row">
  <div class="col-md-12">
    <div class="box">
      <div class="box-header with-border">
        <h3 class="box-title"><%= t('sections.audits.history') %></h3>
      </div>
      <!-- /.box-header -->
      <%= render 'audits/list' %>
    </div>
    <!-- /.box -->
  </div>
</div>
